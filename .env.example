# Server-only. Never expose to the client. Set this in Vercel project settings.
WISEAI_API_KEY=replace_with_your_key

# Optional: set if you self-host the Wise AI cloud endpoint somewhere else.
# WISEAI_UPSTREAM=https://api.wiseai.example.com
